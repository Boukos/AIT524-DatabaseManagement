/*
    Write an SQL query that uses a single-row subquery in a WHERE clause. Explain what the query is intended to do.
    Write an SQL query that uses a multiple-column subquery in a FROM clause. Explain what the query is intended to do.
    Write an SQL query that is based on multiple tables tables and uses a subquery with the GROUP BY statement and HAVING clause. Explain what the query is intended to do.
    Write an SQL query that is based on multiple tables tables and uses a multiple-row subquery in a WHERE clause. The subquery will include the GROUP BY statement and another multiple-row subquery in a HAVING clause. Explain what the query is intended to do.
    Write an SQL query that joins three tables and uses any type of a subquery. Explain what the query is intended to do.
*/